---
repository: SolidRhino/podimo-rss
branch: go-rewrite
commit: 125900d
date: 2026-05-27T16:30:14Z
author: pi
status: approved
verification:
  Q1: Verified
  Q2: Verified
  Q3: Falsified (dropped)
  S1: Falsified (dropped)
  S2: Falsified (dropped)
---

# Code Review — Working Tree (uncommitted)

## Scope

| Item | Value |
|------|-------|
| Strategy | working-tree |
| Changed files | 6 |
| Added lines | +11 |
| Removed lines | −19 |
| In-scope files | AGENTS.md, config.example.yaml, config.go, main.go, main_test.go, podimo/client.go |

Removed three unimplemented/undead video config fields (`VideoEnabled`, `VideoCheckEnabled`, `VideoTitleSuffix`) and wired `StoreTokensOnDisk` so that token caching is skipped when the flag is `false`.

---

## 🔵 Suggestions

### Q1 — `main.go:524` — `if a.cfg.StoreTokensOnDisk {` — untested `false` branch

The `checkAuth` helper now conditionally passes `nil` for `tokenCache` when `StoreTokensOnDisk` is `false`. `main_test.go` always initializes `StoreTokensOnDisk: true`, so the nil-tokenCache path in `checkAuth` is unexercised. 

- **Risk**: low — the branch is a single-line conditional with obvious behaviour.
- **Suggested action**: add a test in `main_test.go` that sets `StoreTokensOnDisk: false` and asserts that `a.tokenCache.Set` is never called after a successful login.

### Q2 — `podimo/client.go:63` — `if tokenCache != nil {` — nil-tokenCache path untested

`NewPodimoClient` now guards `tokenCache.Get(key)` behind a nil check. Every existing test in `podimo/client_test.go` passes a non-nil `FileCache`, so the new nil-guard path is never exercised.

- **Risk**: low — the guard prevents a panic on nil dereference; the nil-`tokenCache` flow is covered implicitly by `main.go` when `StoreTokensOnDisk` is `false`.
- **Suggested action**: add a test in `podimo/client_test.go` that passes `nil` for `tokenCache` and asserts the constructor returns a valid client that falls through to the login path.

---

## Precedents

| Hash | Subject | 30d-follow-ups | Note |
|------|---------|----------------|------|
| `4539b58` | feat: rewrite entire service from Python to Go | 4 | Dead config fields (`VideoEnabled`, `VideoCheckEnabled`, `VideoTitleSuffix`, `StoreTokensOnDisk`) were introduced during rewrite; first follow-up fix arrived within minutes |
| `01647d5` | fix: address validation findings — dead code and structured logging | 0 | Added nil-safe defaults to `PodimoClient`; every call site had to be updated |
| `77a2003` | feat(config): strict env validation with clear error messages | 0 | `StoreTokensOnDisk` identified as "dead in main.go" but explicitly retained per design decision |
| `3ef7335` | feat(config): hybrid config loading with Viper | 1 | Dead fields kept in same commit; design doc stated "Removing them breaks any .env that sets ENABLE_VIDEO, etc." | 

**Composite lessons:**
- Large rewrites and refactors reliably leave dead code behind; expect follow-up sweeps.
- Config struct changes have the widest blast radius — one removed field cascades to docs, examples, `.env`, tests, and Docker.
- Nil-safety changes to constructors propagate to every call site; batching with test updates avoids repeated cross-file churn.

---

## Reconciliation Notes

- **Q3** (`config.go:71` — SetDefault inconsistency on video fields) was **Falsified** at verification: the cited line `v.SetDefault("video_enabled", false)` no longer exists in `config.go` at HEAD because the fields were removed entirely in this diff.
- **S1** (`main.go:539` — path traversal via `FileCache.Set`) was **Falsified** at verification: `TokenKey` returns a 64-character SHA-256 hex digest (`podimo/client.go:121-127`) that can never contain directory separators or path traversal sequences.
- **S2** (`podimo/client.go:64` — path traversal via `FileCache.Get`) was **Falsified** for the same reason as S1.
- **InScope filter**: `InScopeFiles = ChangedFiles` for working-tree strategy; no sidecar files to drop.

---

## Recommendation

**Approved.** The diff is a clean, low-blast cleanup:

1. Removes three undead config fields that were parsed but never consumed by any application logic.
2. Wires `StoreTokensOnDisk` so that `tokenCache` is passed to `NewPodimoClient` only when the flag is `true`, matching the documented intent.
3. Adds a nil guard in `NewPodimoClient` so that disabling disk token storage does not panic.
4. Updates tests to preserve the existing harness behaviour by explicitly setting `StoreTokensOnDisk: true`.
5. Scrubs documentation and example YAML to match the removed fields.

No runtime defects were found. The two remaining findings are test-coverage gaps that can be addressed in a follow-up test PR or deferred since the branches are trivial and protected by obvious nil/conditional guards.

> **Next step:** Add tests for `StoreTokensOnDisk=false` (main_test.go) and `tokenCache=nil` (podimo/client_test.go) to close the coverage gaps before the next broader refactor.

---

## Impact

No inbound consumer references require migration — the removed fields were never read, so no handler, template, or route logic changed. The `StoreTokensOnDisk` wiring changes runtime behaviour only for users who explicitly set the flag to `false` (default remains `true`); for them, tokens are no longer written to `FileCache` files.
