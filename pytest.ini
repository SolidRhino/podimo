[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests

[tool:pytest]
filterwarnings =
    ignore::DeprecationWarning:feedgen
    ignore::DeprecationWarning:quart
